import { expect } from 'chai';
import { register, login } from '../../src/services/authService.js';

describe('Auth Service', () => {
  it('should register a new user (stub)', async () => {
    expect(true).to.equal(true); // replace with real test
  });

  it('should login a user (stub)', async () => {
    expect(true).to.equal(true); // replace with real test
  });
});